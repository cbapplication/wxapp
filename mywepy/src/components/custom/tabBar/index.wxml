<view class="page">
    <view class="tab-bar">
      <view bindtap="tab" class="view {{tabId==index?'active':''}}" wx:for="{{items}}" wx:key="this" data-index="{{item.id}}">{{item.value}}</view>
    </view>  
</view>